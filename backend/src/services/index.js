/**
 * Social Mimic - Services Index
 * @description Exporta todos los servicios de la aplicación
 */

const { TwitterBotService } = require('./twitter-bot.service');

module.exports = {
  TwitterBotService,
};
